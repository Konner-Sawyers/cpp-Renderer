#include "Quad_Tree.h"
#include <stdio.h>
#include <iostream>

/*
int Quad_Tree::max_value_x(int data_array[][2]) {
	int max = 0;

	for (int i = 0; i < 8; i++) {
		if (abs(data_array[i][0]) > abs(data_array[max][0])) {
			max = i;
		}
	}
	return abs(data_array[max][0]);
}

int Quad_Tree::max_value_y(int data_array[][2]) {
	int max = 0;

	for (int i = 0; i < 8; i++) {
		if (abs(data_array[i][1]) > abs(data_array[max][1])) {
			max = i;
		}
	}
	return abs(data_array[max][1]);
}

Quad_Tree::Quad_Tree(int (&data_array)[array_rows][array_columns]) {
	max_x = Quad_Tree::max_value_x(data_array);
	max_y = Quad_Tree::max_value_y(data_array);
}
*/